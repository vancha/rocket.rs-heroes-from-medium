table! {
    heroes (id) {
        id -> Integer,
        fantasy_name -> Varchar,
        real_name -> Nullable<Varchar>,
        spotted_photo -> Varchar,
        strength_level -> Integer,
    }
}
